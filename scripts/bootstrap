#!/usr/bin/env bash

set -euo pipefail

cd -- "$(dirname -- "$0")/.."

echo "==> Running dotnet tool restore"
dotnet tool restore

echo "==> Restoring locked dependencies"
dotnet restore --locked-mode
